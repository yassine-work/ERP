@model ERP.Models.AdvantageType

@{
    ViewData["Title"] = "Delete Advantage Type";
}

<div class="container mt-4">
    <h2 class="text-danger">Delete Advantage Type</h2>

    <div class="alert alert-warning mt-3">
        Are you sure you want to delete this advantage type?
    </div>

    <div class="card">
        <div class="card-body">
            <strong>@Model.AdvantageTypeName</strong>
        </div>
    </div>

    <form asp-action="Delete" method="post" class="mt-3">
        <input type="hidden" asp-for="AdvantageTypeId" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>
